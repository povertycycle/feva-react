import { Settings } from "@/interfaces/settings";

export const CANVAS_WIDTH = 6000;
export const SECTION_HEADER_WIDTH = 400;
export const DEFAULT_SETTINGS: Settings = {
    timeInterval: 1000,
    intervalGap: 100,
};
